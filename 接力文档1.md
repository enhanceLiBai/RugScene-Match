# 接力文档 1

## 当前服务

- 稳定服务：`http://127.0.0.1:8000`，客服和旧后台使用，公网隧道连接此端口。
- 开发服务：`http://127.0.0.1:8001`，新后台地址为 `http://127.0.0.1:8001/admin/v2`。
- 稳定代码目录：`D:\RugScene-Match-main`。
- 开发代码目录：`D:\RugScene-Match-main\dev-runtime`。
- 两套服务共用数据库、图片目录和模型缓存。
- 开发服务不使用守护或自动重启逻辑。

开发服务启动：

```powershell
.ven\Scripts\python.exe scripts\run-dev-service.py
```

稳定服务由 `scripts\run-service.py` 守护，固定监听 8000，并保留自动重启和日志机制。隧道脚本 `scripts\watch-tunnel.ps1` 固定连接 8000，地址记录在 `runtime-logs\tunnel-url.txt`。

## 已完成

- React、ReactDOM、Ant Design、Vite 已安装到 `admin-ui`。
- React 构建产物已复制到 `admin-ui-dist`，FastAPI 提供 `/admin/v2` 和 `/admin/v2/assets`。
- Vite 已配置 `base: '/admin/v2/'`，页面白屏问题已修复。
- 新后台基础导航已完成：客服数据、异常待处理、买家秀库占位、客服子账户占位。
- `GET /api/admin/statistics` 已支持 `today`、`7d`、`30d`、`all`、`custom`。
- 客服数据页面已有匹配次数、成交次数、转化率、趋势表和时间筛选。
- 客服检索无结果会自动创建异常记录。
- 异常接口已完成：
  - `GET /api/admin/exceptions`
  - `GET /api/admin/exceptions/{id}`
  - `POST /api/admin/exceptions/{id}/resolve`
- 客服子账号维持现有功能。

## 测试情况

- 后端编译检查通过。
- `tests\test_conversions.py tests\test_access.py`：4 项通过。
- React + Ant Design `vite build` 通过。
- 8000 `/health`、8001 `/health`、8001 `/admin/v2` 均已验证返回 200。
- 自定义统计接口已进入管理员鉴权流程，未登录访问返回 401。

## 当前待用户验收

需求：客服数据时间筛选。

请在 8001 页面验证：

1. 今日、近 7 天、近 30 天、全部。
2. 选择“自定义”后日期控件可用。
3. 选择开始和结束日期后统计结果刷新。
4. 未来日期不可选。

用户确认后，在 `docs\admin-project-schedule.md` 中勾选该任务。

## 已验收标签

- 【已验收】客服数据时间筛选：今日、本周、本月、近 7 天、近 30 天、全部、自定义日期及未来日期限制。
- 【已验收】客服数据总览：匹配、成交、转化率卡片，匹配/成交趋势图及每日明细。
- 【已验收】客服排行和单客服详情：客服排序、个人统计、趋势、历史分页和历史详情。

以上三项已在 `docs\admin-project-schedule.md` 对应排期标题标记为【已验收】。

## 后续顺序

1. 异常待处理筛选和详情完善（开发中，尚未验收）。
2. 买家秀图库列表、搜索、预览和分页（开发中，尚未验收）。
3. 图片删除和历史引用提示。
4. 商品数据统计。
5. 沙发颜色、地板颜色筛选。
6. 客服子账户页面接入现有功能。
7. UI 统一和最终切换。

## 继续开发时必须遵守

- 不实现 AI 初筛 + 产品部二筛流程。
- 数据库和图片目录按要求共用。
- 新功能只在 8001 开发和验收，不重启 8000。
- 每完成一个需求，必须执行功能测试和直接相关的 Bug/回归测试。
- 测试通过后交给用户实际验证，用户确认后再勾选排期任务。


## 2026-09-21 接力开发记录：时间筛选补齐，待用户验收

- 接力核对发现：dev-runtime 中原先未包含文档声称完成的自定义日期功能。本轮已在开发目录补齐，未同步到稳定服务代码。
- 已支持今日、本周（周一至今）、本月（月初至今）、近 7 天、近 30 天、全部、自定义日期。
- 日期统计采用北京时间，包含开始和结束当天；前后端均限制未来日期。
- 自定义未选完整时提示选择日期，不展示上一次范围的数据；切换范围时避免旧请求覆盖新结果。
- 验证：开发目录 tests/test_conversions.py、tests/test_access.py 共 5 项通过；Vite 构建通过。
- 8001 页面实际操作验证：自定义 2026-09-20 至 2026-09-21，统计刷新为 3 次匹配、0 次成交，趋势显示对应两天。
- 仅重启 8001；8000 未重启。页面地址：http://127.0.0.1:8001/admin/v2。
- 排期复选框保持不变，等待用户验收。验收后下一项为客服数据总览卡片和趋势图完善。

### 日期控件中文修复
- 开发后台已配置 Ant Design 简体中文和 Day.js 中文语言包，星期从周一开始。
- Vite 构建通过；8001 浏览器确认星期及月份切换提示显示中文。只更新开发前端，无需重启服务。


## 2026-09-21 客服数据总览接力记录

- 用户已确认时间筛选（含中文日期控件）无问题，排期中 9 月 21 日任务已勾选。
- 本轮仅开发 dev-runtime 前端：完善匹配次数、成交次数、转化率卡片，加入统计口径与加载状态；增加每日匹配/成交双折线趋势图，缺失日期补零，保留每日明细并按 10 条分页。
- 统计口径沿用现有接口：成交归属原匹配日期，转化率为所选匹配记录中的成交比例。
- 验证：test_conversions.py 两项通过（包含统计核对及空数据），Vite 构建通过；8001 浏览器验证趋势图、补零日期、自定义未选择日期和 2026-09-15 空数据范围。切换自定义时发现并修复空日期导致渲染失败，修复后重新构建并复验通过。
- 前端静态产物已更新，无服务重启。总览需求待用户验收，未勾选对应任务。
- 用户验收后下一项：客服排行和单客服详情。


### 客服数据总览用户验收通过

用户确认本次总览无问题，排期中客服数据总览任务已全部勾选。下一项为客服排行和单客服详情。


## 客服排行和单客服详情：已开发，待用户验收

- 开发目录：dev-runtime；仅更新 8001，未重启或修改 8000 服务代码。
- 客服数据页底部新增客服排行，支持按匹配次数、成交次数降序排序，展示转化率和零匹配客服。
- 点击“查看详情”打开个人统计、趋势及匹配历史；沿用主页面的时间范围，可在详情中切换客服。
- 个人历史每页 20 条，展示客户图片、结果数、成交登记及排名；可打开当次完整匹配结果及图片预览。
- 复用现有 /api/history/{id} 和图片接口；扩展 /api/admin/statistics 返回 ranking、history（仅指定客服时返回历史），history_page 控制分页。
- 验证：tests/test_conversions.py tests/test_access.py 共 6 项通过，覆盖多客服统计、自定义范围、分页、图片和结果快照、权限；Vite 构建通过。浏览器已验证排行排序、个人统计和实际历史图片/结果展示。
- 当前待用户验收，排期对应复选框未勾选。验收后下一项：异常待处理筛选和详情完善。


### 8001 服务恢复

用户反馈无法访问；检查确认 8001 进程已退出，8000 正常。已重新启动开发服务，`/health` 返回 200，访问地址恢复。


### 异常列表图片交互调整

列表不再加载或展示大图，仅显示已保存提示；管理员点击“查看”进入详情抽屉后查看客户原图、场景和状态。待处理列表标记处理后自动刷新移除；可切换“已处理/全部”查看。


### 买家秀图库列表开发记录

- 已接入现有 `/api/library`，新增后台图库列表：缩略图预览、SKU/商品名称搜索、尺寸、风格/颜色、分页和空状态。
- 仅更新 8001 前端静态产物，Vite 构建通过；未执行删除或修改图库数据。
- 待用户验收；后续继续图片删除和历史引用提示。

### 新接力开发入口：异常待处理与图库稳定性

- 当前优先处理异常待处理接口/筛选/详情的稳定性，确保已有异常记录可见，列表不加载大图，点击详情才展示原图和具体信息。
- 图库当前已能返回数据库图片，但前端/API 字段映射和静态资源更新仍需继续验证；商品 ID 来自 `product_images.product_id`，商品名称按已确认规则展示。
- 每个模块完成后先运行直接相关测试，再交用户验收；未收到用户确认前不勾选排期任务。

## 2026-09-22 异常待处理与图库列表稳定性

- 异常页已接入状态、客服和日期范围筛选，未来日期不可选；标记处理后会按当前筛选刷新，已处理记录不再显示重复处理按钮。
- 异常列表不加载大图；仅在历史照片实际存在时显示“已保存”，详情抽屉按需展示原图、客服、时间、状态、提示和场景标签。详情请求失败会给出提示。
- 图库接口已返回真实上传时间；列表支持商品 ID、商品名称和 SKU 搜索，分别展示商品 ID/名称与 SKU，并使用预览图加载缩略图。
- 最小验证通过：后端编译检查；异常流程与图库列表契约测试共 2 项；Vite 生产构建。既有第三方弃用警告和构建包体提示不影响本次功能。
- 新构建产物已同步到 `dev-runtime\admin-ui-dist`。仅重启 8001 开发服务，8000 未操作；`http://127.0.0.1:8001/health` 返回正常。
- 当前待用户在 `http://127.0.0.1:8001/admin/v2` 验收异常筛选/详情和图库列表；未勾选排期任务。

### 商品名称字段修复

- 已确认历史导入数据将商品名称写在 `images.style`，`images.product_name` 全部为空。
- 在共享数据库中将 `product_name` 为空且 `style` 非空的 899 条图片复制回填；未覆盖任何已有名称，原 `style` 保留不变。
- 开发服务的商品检索结果已增加独立 `product_name` 字段；客服前台和历史详情优先展示 `product_name`，旧历史结果继续回退读取 `style`。
- 数据复核：899 条图片具有商品名称，回填记录的 `product_name` 与 `style` 一致。前端脚本语法检查和历史详情 2 项测试通过。
- 仅更新并重启 8001 开发服务；8000 稳定服务未修改、未重启。

### 图片删除与历史引用提示

- 图库列表新增“历史引用”列，按历史匹配记录统计每张买家秀的引用次数，同一条历史中重复出现只计一次。
- 删除确认弹窗会显示实际引用数，并明确删除后历史结果快照仍保留；图库图片、向量、场景标签和商品关联按现有逻辑删除。
- 最小验证通过：历史引用计数及删除后快照保留测试 1 项；Vite 生产构建通过。
- 仅更新 8001 开发版，8000 稳定服务未修改。

### 图片删除功能用户验收通过

- 用户已确认图片删除和历史引用提示功能验收通过。
- `docs\admin-project-schedule.md` 中 2026 年 9 月 26 日的图片删除功能已全部勾选，并标记为【已验收】。
- 下一项开发功能：商品数据统计。

### 商品数据统计已开发，待用户验收

- 新后台已增加独立“商品数据”页面，支持按商品 ID 或商品名称搜索。
- 时间范围支持今日、本周、本月、近 7 天、近 30 天、全部和自定义日期，日期归属沿用原匹配时间。
- 页面展示当前图库买家秀数量、匹配成功次数、成交次数、转化率和按商品拆分的明细表。同一商品在一条匹配历史中重复出现只计一次。
- 直接相关端到端测试 1 项通过，覆盖商品搜索、日期范围、买家秀数量、重复结果去重、成交数和转化率；Vite 生产构建通过。
- 8001 页面已实际验证总览、明细和“佩尼达公寓”名称搜索；8000 稳定服务未修改。

### 商品数据统计用户验收通过

- 用户已确认商品数据统计功能验收通过。
- `docs\admin-project-schedule.md` 中 2026 年 9 月 27 日的商品数据统计已全部勾选，并标记为【已验收】。
- 下一项开发功能：商品分类筛选（沙发颜色、地板颜色）。

## 2026-09-23 后续未完成事项

### 商品统计明细买家秀预览图：未解决

- 用户反馈“商品数据”中的“商品统计明细”买家秀栏目仍未显示预览图，本问题暂不视为已修复。
- 当前前端已尝试使用 `/api/images/{image_id}?preview=1` 和 Ant Design 图片预览组件，但用户实际页面仍未看到图片。
- 下一次接力必须先从浏览器实际请求和接口响应开始排查：确认 `/api/admin/product-statistics` 返回的 `image_id/image_url`、图片请求状态码、响应类型及图片存储路径；不要仅凭构建成功或服务健康检查判定完成。
- 在用户实际确认预览图可见前，不得勾选相关验收项。

### 商品分类筛选：暂搁置

- 沙发颜色、地板颜色筛选暂时搁置，不计入已验收功能。
- 现有分类筛选代码保留，后续重新启用时需先确认接口统计口径和页面交互，再进行测试和用户验收。

### 客服子账户：已开发，待用户验收

- 新后台“客服子账户”已接入现有 `/api/accounts` 列表和新增客服账号接口。
- 页面支持账号列表、角色展示和新增客服账号；密码和账号校验沿用现有后端规则。
- Vite 构建通过，8001 已重启验证健康状态；尚未完成用户验收，排期不得勾选。

### 当前接力顺序

1. 优先解决商品统计明细买家秀预览图，并完成浏览器网络请求级验证。
2. 用户确认预览图正常后，再进行客服子账户页面验收。
3. 商品分类筛选继续保持搁置。


## 2026-09-23 商品统计预览图修复：已部署，待页面确认

- 发现 8001 后端进程启动于 13:59，早于 conversions.py 的 15:06 更新；此前新增图片字段的代码未随前端更新加载。因当前有效管理员登录不可用，未取得重启前真实统计响应，旧进程缺字段仍是待最终页面确认的原因判断。
- 直接相关测试复现预览图改动引入的统计回退：按图片拆行导致原按商品聚合的数量和旧历史匹配数错误。已恢复已验收的按商品去重统计，每个商品返回一张当前有效买家秀的 image_id/image_url；无图库图片的历史商品保留统计，图片字段为空。
- 前端行键恢复 product_id，移除指向不存在文件的图片 fallback。分类功能未改动。
- 最小测试：test_admin_product_statistics_search_dates_and_totals 通过，覆盖统计、搜索、日期、预览图 HTTP 200/image/jpeg 和可解码、原图 HTTP 200/image/png、无图片历史商品。Vite 构建通过。
- 真实存储检查：2360 条有效买家秀关联的文件路径全部存在且位于图库目录；抽样原图可解码。
- 构建产物已同步 dev-runtime/admin-ui-dist，仅重启 8001；新进程 40708，8000 进程仍为 14304。开发页面引用新构建 index-CXlaoWYQ.js，两端健康检查返回 200。
- 验证限制：浏览器工具报 unsupported Codex auth method: apikey；项目保存的管理员登录信息返回 401。尚未完成登录后真实商品统计接口/图片请求和页面可见性验证，不得声称用户已验收，不勾选排期。请在 8001 刷新“商品数据”确认缩略图及点击大图；如仍失败，获取实际统计 items 和图片网络响应继续定位。

### 商品统计明细改为单张买家秀排行

- 按用户确认的口径，商品统计明细不再按相同商品聚合，每张当前有效买家秀独立一行。
- 匹配次数按 `buyer_image_id + history_id` 去重统计，成交通过历史结果排名归到具体 `buyer_image_id`；缺少图片 ID 的旧结果不强行分摊到某张图片。
- 明细按单张图片匹配成功次数降序排列，同次数按图片 ID 保持稳定；商品 ID/名称搜索和日期范围继续生效。
- 顶部买家秀数量为当前筛选后的有效图片数，匹配、成交和转化率为这些单图数据的汇总。
- 直接相关统计测试通过，Vite 构建通过。构建产物已同步并仅重启 8001，8000 进程未变。
- 8001 实际页面验证通过：同一商品可显示多张图片，首屏单图匹配次数按 12、10、9 等降序排列，图片、成交数及单图转化率正常展示。
