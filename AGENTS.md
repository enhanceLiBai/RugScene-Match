# 当前项目 Agent 开发约定

本文件仅适用于当前项目。目标是尽快、稳定地交付已确认的业务功能，避免因低概率假设、过度防御或重复验证扩大开发范围。

## 一、工作优先级

- 以用户明确要求、已确认的业务事实和验收标准为准。
- 通用技能、模板和最佳实践用于辅助实施，不得自行扩大产品范围。
- 先完成端到端业务闭环，再处理确有价值的优化。
- 已确认的业务假设应直接用于工程取舍，不要反复挑战或重新论证。

## 二、实施范围

- 只实现当前需求及其必要依赖；不顺手重构无关代码。
- 优先采用项目已有结构和简单、可维护的实现。
- 不主动引入分布式锁、复杂重试框架、额外迁移体系、权限系统、通用抽象层或大规模防御代码，除非：
  - 用户明确要求；
  - 问题已经复现；
  - 在当前实际业务条件下有较高发生概率；
  - 会造成数据丢失、敏感信息泄露或核心流程不可用。
- 理论上可能、但依赖极端前提或不符合当前业务模型的问题，可记录为后续建议，不得阻塞交付。
- 对低影响、易回退的改动，直接采用最小实现，不为假设性风险增加复杂设计。

## 三、项目当前业务假设

- 预计同时使用的客服不超过 8 人，且不同客服面对的客户通常不同。
- 客户用于检索的查询图片不入图库、不持久化，不与其他客户共享可变状态。
- 多人同时导入字节完全相同的卖家秀图片不属于预期业务流程。
- 卖家秀元数据允许缺失，所有元数据字段均为选填；缺失元数据不得阻止上传、入库、展示或检索。
- 元数据当前不参与相似度排序。
- 当前目标是内部业务功能和稳定运行，不按开放互联网的高对抗服务设计。

若用户以后明确改变上述条件，再针对新增风险调整设计；不要提前建设。

## 四、测试与验证

- 测试重点：主流程、常见失败路径、已复现缺陷、关键数据写入和前后端契约。
- 新功能或修复采用最小有效验证：先运行相关测试，再运行一次与改动规模相称的回归测试。
- 每个改动周期最多执行一次相关测试和一次适度回归；只有代码继续变化、出现新失败或获得新证据时，才重新运行受影响的测试。
- 不为实现细节、纯理论竞争时序、恶意构造输入或每个不可能分支单独补测试。
- 不因没有代码变化而重复运行同一套测试；只有出现新失败或新证据时才扩大验证。
- 第三方依赖的既有弃用警告不阻塞交付，除非本次改动新增或明显恶化了它。
- 除非用户明确要求，不做大规模压力测试、渗透测试、模糊测试或穷举并发测试。

## 五、审查与缺陷分级

- 阻塞问题：当前验收标准未满足、常见用户路径不可用，或在实际业务条件下很可能导致数据丢失、敏感信息泄露、核心服务不可用。
- 非阻塞问题：低概率竞态、极端输入、风格偏好、未来扩展性、可选重构及理论性加固。
- 审查意见必须说明触发条件、影响范围和可验证证据；不能只描述“可能存在风险”。
- 若问题成立依赖于当前业务假设之外的条件，应列为建议或向用户确认，不得直接升级成必须修复项。
- 默认进行一轮实现审查和一轮修复复查。继续增加修复轮次，必须有与当前需求直接相关的新证据。

## 六、任务入口与历史文档

- 当前任务只由用户本轮明确要求、用户明确指定的验收标准或明确重新启用的实施计划产生。
- `docs/superpowers/plans/` 和 `docs/superpowers/specs/` 中标记为“已完成”“已实施”或“历史”的文件仅供追溯，不是待办清单；其中未勾选的复选框不得被解释为需要继续实现、补测或审查。
- `.superpowers/` 是本机历史执行记录和临时工作文件，不作为新任务的需求或验收来源。
- 不主动从历史报告、旧审查意见、提交记录或测试名称中派生新需求。发现与当前任务无关的问题时，只在确有业务影响时简短记录，不进入修复循环。
- 创建或切换 Git worktree 后，应先确认当前工作目录能读取本文件；若不能，先恢复项目约束，再开始实施。

## 七、最低工程底线

- 不提交或输出 `.env`、密码、令牌等敏感信息。
- 不执行未经用户授权的破坏性数据操作。
- 对上传图片保留必要的格式、大小和可解析性检查。
- 文件操作限制在明确的项目或存储目录内，数据库正常路径保持基本事务一致性。
- 改动完成后至少验证目标业务流程可运行、相关测试通过、文档与实际行为一致。

## 八、停止条件

满足以下条件即可交付，不继续主动寻找边缘问题：

- 用户要求的业务闭环已实现；
- 已知且与当前范围直接相关的阻塞问题已解决；
- 相关测试通过，并完成一次适度回归；
- 剩余事项均为已明确标注的非阻塞建议。
