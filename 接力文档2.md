# 接力文档 2

## 当前开发环境

- 稳定服务：`http://127.0.0.1:8000`，客服和旧后台使用，公网隧道连接此端口。
- 开发服务：`http://127.0.0.1:8001`，新版后台地址为 `http://127.0.0.1:8001/admin/v2`。
- 稳定代码目录：`D:\RugScene-Match-main`。
- 开发代码目录：`D:\RugScene-Match-main\dev-runtime`。
- 两套服务共用数据库、图片目录和模型缓存。
- 后续开发和验收只操作 8001，最终切换前不修改或重启 8000。

开发服务启动命令：

```powershell
.ven\Scripts\python.exe scripts\run-dev-service.py
```

## 2026-09-23 已确认状态

### 已验收

- 客服数据时间筛选、总览、趋势、每日明细、客服排行和单客服详情。
- 图片删除、二次确认和历史引用提示。
- 商品数据搜索、时间筛选和顶部汇总。
- 商品统计明细：每张有效买家秀独立一行，匹配次数按 `buyer_image_id + history_id` 去重，按单图匹配成功次数降序排列；成交通过当次结果排名归到具体图片。
- 商品统计明细的缩略图、图片预览、单图成交次数和单图转化率。
- 客服子账户：账号列表、新增客服账号、角色展示及现有校验。

商品统计直接相关测试和 Vite 构建已通过，8001 实际页面已确认同一商品可显示多张图片，首屏按单图匹配次数降序展示。

### 未完成，暂搁置

- 商品详情筛选（沙发颜色、地板颜色）六项需求均未完成验收，暂时搁置。
- 页面入口已与商品数据分离，位于“买家秀库 > 商品数据 > 商品分类筛选”子页；商品数据的另一子页为“商品统计明细”，不再渲染分类长列表。
- 当前仅有颜色下拉框和实验性列表；分类图片数量没有展示。
- 当前接口按商品 ID 统计匹配和成交，再把相同数字赋给该商品的每张图片，造成重复计数；分类内排序因此不是单张图片的真实匹配排行。
- 分类前端没有传递商品数据页的时间范围，接口始终使用自己的默认时间参数。
- 现有前后端分类代码保留，不删除、不继续完善，也不作为当前第一版切换的阻塞项。
- 用户重新启用时，需要按 `buyer_image_id + history_id` 统计单图匹配，通过历史结果排名归属单图成交，展示所选分类图片总数、匹配数和转化率，再按单图匹配次数排序并补直接相关测试。

### 已开发，待用户验收

- 异常待处理：状态、客服和日期筛选，详情抽屉，客户图片和场景标签，标记处理。
- 总图库：缩略图、预览、商品 ID/名称/SKU、上传时间、搜索和分页。

## 后续开发顺序

### 任务 1：异常待处理验收收口

1. 验证待处理、已处理、全部状态筛选，以及客服和日期筛选。
2. 验证详情中的客户原图、客服、时间、状态、提示和场景标签。
3. 验证标记处理后按当前筛选刷新，已处理记录不再出现重复处理按钮。
4. 核对功能清单中尚未明确实现的筛选条件、候选数量和完整结果快照；只有用户确认需要时才补齐。
5. 运行异常主流程、权限和状态变更的直接相关测试，用户确认后标记验收。

### 任务 2：总图库验收与缺口确认

1. 验证缩略图、图片预览、商品 ID/名称/SKU、上传时间、搜索、分页和空状态。
2. 确认功能清单中“单图匹配成功次数”和“上传时间筛选”是否仍属于第一版需求；当前页面尚未实现这两项，不自行扩展。
3. 对用户确认保留的缺口完成最小实现和直接相关测试。
4. 用户确认后标记总图库列表验收；已验收的删除功能不重复测试。

### 任务 3：UI 和公共交互统一

1. 补齐顶部账号信息和退出登录。
2. 统一各页加载、空数据、失败提示和成功提示。
3. 统一 Ant Design 间距、表格、表单和窄屏显示，避免内容重叠。
4. 只验证四个模块的常用路径和本轮改动，不执行全量探索性审查。
5. 用户确认后标记 UI 统一任务验收。

### 任务 4：新版后台切换

1. 前三项验收完成后，准备让新版后台接管 `/admin`，保留可回退方案。
2. 切换前核对管理员登录、刷新、导航和静态资源路径。
3. 运行统计、异常、图库和账号的最小相关回归测试。
4. 先在 8001 完成最终用户验收；获得用户明确确认后再安排 8000 切换，不提前重启稳定服务。

## 文档状态

- `docs/admin-feature-list.md` 已按“已验收 / 已开发待验收 / 暂搁置”更新。
- `docs/admin-project-schedule.md` 已补记商品统计明细验收，并将商品分类筛选标为暂搁置。
- `接力文档1.md` 作为历史实施记录保留；从本文件继续后续开发。

## 继续开发约束

- 不实现 AI 初筛和产品部二筛流程。
- 不重构匹配算法，不增加复杂权限体系。
- 客户查询图片继续随历史结果持久化，但不进入检索图库。
- 卖家秀元数据允许缺失，缺失不得阻止上传、展示或检索。
- 每个需求只运行能证明该功能的直接相关测试；测试通过后交给用户实际验收，确认后再勾选。
