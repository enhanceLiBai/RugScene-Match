# 换机开发交接

## 入口

当前开发基线为 `main`。先阅读 `AGENTS.md`、本文件和 `README.md`；运行步骤见 `启动方式.md`，无需进入旧 worktree。

## 已纳入主分支的代码

- OpenCLIP 编码、PostgreSQL/pgvector 图库、图片上传与相似检索。
- 现有页面与真实后端 API 集成、可选商品元数据、完整结果图片展示。
- 商品主图与买家秀关联持久层、腾讯 XLSX 图片解析、Excel 后台导入服务和进度 API。

商品关联检索与 Excel 前端入口仍需后续开发；合并已有代码不代表旧计划中的全部功能已经完成。

## 任务与设计资料

- `docs/superpowers/specs/`：设计说明。
- `docs/superpowers/plans/`：实施计划。商品关联计划的 Task 1–3 已有实现；Task 4–6 留作后续续接参考，以实际代码和用户后续要求为准。
- `docs/development-history/`：从各 worktree 汇总的任务说明、进度、报告和审查记录。这些都是历史快照，不是自动执行的待办，不应按旧审查记录自行扩展任务。

## 新机器准备

1. 克隆仓库并切换到 `main`。
2. 准备 Python 3.12、Node.js，以及 PostgreSQL 16 和 pgvector。
3. 运行 `scripts/bootstrap.ps1` 安装 Python 依赖。
4. 从 `.env.example` 创建本机 `.env` 并配置数据库连接。
5. 按 `启动方式.md` 初始化数据库、启动服务。

Git 仅迁移代码与文档。虚拟环境、真实 `.env`、模型缓存、图片和数据库数据不在仓库内。需要保留现有图库时，另行迁移数据库与对应的 `data/images`；模型缓存可另行复制或重新下载。

## 本次合并验证记录（2026-09-08）

前端 15 项测试通过；Excel 导入在源分支的 9 项相关测试通过。合并后发现并修正了 Excel 测试缺少缓存目录参数的问题；数据库接口验证因本机 PostgreSQL 连接超时未完成。用户要求本轮仅完成合并与开发资料交接，不继续启动环境或扩展验证。
