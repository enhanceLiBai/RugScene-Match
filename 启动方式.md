# 启动方式

现有功能统一在项目根目录的 `main` 分支运行。在 PowerShell 中执行：

```powershell
cd "D:\电商ai应用"
$env:PYTHONIOENCODING = 'utf-8'
.venv\Scripts\python.exe -m backend.cli init-db
.venv\Scripts\python.exe -m backend.cli serve
```

然后访问 http://127.0.0.1:8000/ 。停止服务时在对应终端按 `Ctrl+C`。

启动前确保 PostgreSQL 正在运行、已安装 pgvector，并在项目根目录的 `.env` 中配置数据库连接。已有服务占用 8000 端口时，先停止旧服务。

模型默认复用项目内 `.cache\open_clip`；也可在 `.env` 中通过 `CLIP_CACHE_DIR` 指定已有缓存目录。环境配置、模型缓存和本机图片数据不提交到 Git 仓库。

页面支持买家秀上传和图片检索。Excel 导入目前通过 `POST /api/imports` 上传（字段名 `workbook`），通过 `GET /api/imports/{job_id}` 查询进度；可访问 http://127.0.0.1:8000/docs 使用接口。
