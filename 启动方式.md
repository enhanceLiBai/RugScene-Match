# 启动方式

排序冷暖判断使用双图独立调用：每次仅发送客户图和一张候选买家秀，Top10 最多发起 10 次请求，每次查询最多 5 路并行。单次模型超时 25 秒，整体等待 30 秒；已完成的结果正常计分，失败或超时的候选不加冷暖分，已有构图缓存继续使用。

场景识别遇到枚举外的沙发颜色时，当前标签仍输出“其他”或“无法判断”，不改变检索逻辑；模型提供的具体颜色描述会追加记录到 `data/sofa-color-observations.jsonl`，供后续人工统计扩展枚举。

## 图库大图清洗

批量删除商品主图：运行 `.ven\Scripts\python.exe scripts\delete_product_main.py` 先预览，追加 `--execute` 才删除。脚本根据数据库 `product_main` 关联选择图片（含历史停用主图），跳过任何同时关联 `buyer_sofa` 的图片；不会按外观识别广告图，也不处理没有商品关联的图片。实际删除会清理图片记录、向量、场景标签、商品关联和原文件，历史快照不改写，但其中对应图片失效。文件清理失败会单独报告。

管理员可访问 `/admin/library`，或在后台“买家秀图库”点击“大图清洗页面”。独立页面每页展示 20 张完整大图，支持翻页、跳页、点击原图和逐张删除。删除沿用现有图库删除接口，不可撤销，历史记录中的对应图片也将无法打开。此页面仅限本机或局域网管理员使用。

## 账号登录与开通

系统不开放注册。客服使用 `/login` 登录，管理员使用内网 `/admin/login` 登录。账号区分管理员和客服；管理员登录后在“客服账号”栏目开通账号，客服无权查看后台。账号为 2–40 个中文、英文字母、数字、点、横线或下划线，不含空格，密码 6–128 字符。

首次使用时，在服务器项目目录的交互 PowerShell 中执行（密码会提示输入两次，不回显，不要通过命令参数或聊天发送密码）：

```powershell
cd "D:\RugScene-Match-main"
.ven\Scripts\python.exe -m backend.cli init-db
.ven\Scripts\python.exe -m backend.cli create-user admin --name "管理员" --role admin
```

创建成功即可访问 `http://127.0.0.1:8000/admin/login`，不需要再次重启服务。以后也可通过 `create-user kefu01 --name "客服姓名"` 本机开通客服，或用 `reset-password kefu01` 重置密码；重置后该账号全部旧会话失效。

登录有效期为7天，退出立即使当前会话失效。客服账号仅保留最近一次成功登录的会话：新设备登录后，旧设备下一次请求会提示重新登录；错误密码不会挤掉现有会话。管理员账号允许多设备同时登录，各自退出不影响其他设备。登录凭证保存在 HttpOnly Cookie，服务重启不丢失有效会话；外网 HTTPS 使用 Secure Cookie，局域网 HTTP 沿用内网直连。不同访问域名的登录状态不共享。

新查询记录由后端自动绑定登录用户，管理员的历史列表和详情显示查询人姓名及账号；下单登记另记提交人。旧记录保持“未关联客服”，不猜测归属。照片匹配算法不处理登录凭证。已有安装执行 `init-db` 会幂等新增账号、会话表和归属字段，保留既有图库与历史记录。

## 本机测试服务守护

当前应用由开机计划任务 `RugScene-App` 托管，应用退出后约 5 秒重新启动，日志在项目根目录 `runtime-logs/service.log`。正常使用时不要另行执行 `backend.cli serve`，以免与守护任务争用 8000 端口。下方手动启动命令仅用于未启用守护任务的环境。

临时隧道由 `RugScene-Tunnel` 任务观察，每 10 秒检查一次。已有隧道保持原进程；退出后重新启动 cloudflared，新地址写入项目根目录 `runtime-logs/tunnel-url.txt`，连接日志在同目录 `tunnel-stderr.log`。恢复可能更换测试地址，不保证原地址继续可用。若要主动关闭隧道，先停止 `RugScene-Tunnel` 任务，再退出 cloudflared，避免被自动启动。

守护任务安装脚本分别为 `scripts/install-service.ps1` 和 `scripts/install-tunnel-watch.ps1`，均需管理员 PowerShell。数据库依赖 Docker Desktop 启动，不由这两个任务管理。

在当前项目根目录使用 `.ven` 虚拟环境。在 PowerShell 中执行：

```powershell
cd "D:\RugScene-Match-main"
$env:PYTHONIOENCODING = 'utf-8'
.ven\Scripts\python.exe -m backend.cli init-db
.ven\Scripts\python.exe -m backend.cli serve
```

本机访问 http://127.0.0.1:8000/；同一局域网设备访问 `http://本机局域网IP:8000/`，当前本机地址为 `http://192.168.124.245:8000/`。停止服务时在对应终端按 `Ctrl+C`。

启动前确保 PostgreSQL 正在运行、已安装 pgvector，并在项目根目录的 `.env` 中配置数据库连接。已有服务占用 8000 端口时，先停止旧服务。

模型默认复用项目内 `.cache\open_clip`；也可在 `.env` 中通过 `CLIP_CACHE_DIR` 指定已有缓存目录。环境配置、模型缓存和本机图片数据不提交到 Git 仓库。

客服首页用于图片匹配、查看自己的查询记录和补登记下单。管理员访问 `http://127.0.0.1:8000/admin`，使用“下单成功 / 买家秀图库 / 历史匹配记录 / 客服账号”四个栏目；原 `/feedback` 地址仍可打开同一管理后台。图库上传、Excel 导入、补充场景标签、刷新和删除图片都在后台操作，历史记录在后台直接展开客户照片与当时推荐结果。

Excel 导入接口为 `POST /api/imports`（文件字段名 `workbook`），通过 `GET /api/imports/{job_id}` 查询进度；可访问 http://127.0.0.1:8000/docs 使用接口。

## 下单成功登记

- 客户未下单时无需填写、无需提交，不生成成交记录。
- 客户下单后，在结果底部选择本次 Top 排名，自动带出商品 ID（只读），点击“登记下单成功”。服务器从原查询快照读取商品 ID，不接受手填商品。少量无商品 ID 的推荐仍可按排名登记。
- 客服可展开首页“我的查询记录”，查看本人历史照片与推荐结果，补登记下单；不能读取其他客服的记录。旧记录未关联客服的，不放入个人查询列表。
- 同一查询 ID 只计一次成功转化。重复提交相同排名返回已有登记，不新增；已登记后不支持改选另一排名。重复上传同一照片重新检索属于新的查询。
- 管理后台“下单成功”只展示成交记录，可查看客服、Top 排名、商品 ID、匹配时间、登记时间及原结果。转化率分母为所有已关联账号的查询次数（包括无结果查询），分子为已登记成功的查询次数，不代表订单数；未关联账号的旧查询不纳入转化率。
- 旧推荐反馈数据保留，但前后端不再展示原评价表单与反馈列表。
- 新增 `match_conversions` 表，部署时执行 `init-db` 后重载应用；保留已有账号、图片和查询记录。

推荐卡片在商品 ID 下方显示已保存的款式名称，没有款式时不显示此行。历史快照不补写原来未保存的款式信息。

### 后台访问限制

历史匹配记录可按“全部、今天、近三天、近七天、1个月内”筛选。按北京时间当天零点计算，包含今天，“1个月内”为近30个自然日；加载更多保留当前时间条件。

历史记录和下单详情中的推荐卡片支持“复制图片”（相似买家秀）及“复制商品 ID”（有 ID 时显示）。本机 localhost/127.0.0.1 可使用浏览器图片剪贴板接口；局域网 HTTP 若不支持该接口，按钮会提示右键买家秀图片复制。

管理员请使用 `http://127.0.0.1:8000/admin` 或 `http://服务器局域网IP:8000/admin`，不要使用隧道域名。允许的直连来源为回环地址、IPv4 私有网段及 IPv6 本地地址；如果网络路由连通其他私有网络，这些直连来源也在允许范围内。

服务启动时关闭 Uvicorn 代理头解析，后端不信任转发头声明的内网地址。带 Cloudflare 或代理转发标记的请求不能访问管理功能。当前 Cloudflare 隧道无需另配规则；以后更换代理时应保留转发标记并验证后台仍返回 403。新接口默认仅限内网，只有客服需要的接口才加入 `backend/access.py` 的公网白名单。
- 更新后执行上方 `init-db` 命令以补建 `match_feedback` 表，再重启后端。初始化保留已有数据。若当次历史保存失败，页面会提示无法提交反馈，需重新匹配。

推荐卡片左侧展示本次客户照片、右侧展示相似买家秀，不再展示商品主图。客服页面使用本次提交照片的独立预览，历史记录和下单详情使用对应记录保存的客户照片；旧记录未保存照片时显示缺失提示。
